# 产品需求文档 (PRD) - 水印文件本地应用

## 1. 项目概述

**项目名称**: 水印文件本地应用 (WatermarkApp)
**目标平台**: Windows
**核心价值**: 为用户提供一个简单、高效、可定制的本地图片加水印工具，支持批量处理，保护图片版权。

## 2. 功能需求

### 2.1 文件处理
- **导入**:
  - 支持单张图片拖拽或通过文件选择器导入。
  - 支持批量导入多张图片或整个文件夹。
  - 界面实时显示已导入图片的缩略图和文件名列表。
- **支持格式**:
  - 输入: JPEG, PNG (必须支持透明通道), BMP, TIFF。
  - 输出: JPEG, PNG。
- **导出**:
  - 用户可指定输出文件夹，禁止导出到原文件夹以防覆盖。
  - **命名规则**:
    - 保留原文件名。
    - 添加自定义前缀。
    - 添加自定义后缀。
  - **高级功能**:
    - JPEG 质量调节 (0-100)。
    - 导出时调整图片尺寸（按宽度、高度或百分比）。

### 2.2 水印类型
- **文本水印**:
  - 自定义文本内容。
  - 可调节透明度 (0-100%)。
  - **高级功能**:
    - 选择系统字体、字号、粗体、斜体。
    - 通过调色板选择颜色。
    - 添加阴影或描边效果。
- **图片水印 (高级功能)**:
  - 从本地选择图片作为水印。
  - 支持带透明通道的 PNG 图片。
  - 可按比例或自由调整大小。
  - 可调节整体透明度 (0-100%)。

### 2.3 布局与样式
- **实时预览**: 所有水印调整在预览窗口实时生效，可切换预览不同图片。
- **位置**:
  - 提供九宫格预设位置。
  - 支持在预览图上鼠标拖拽。
- **旋转 (高级功能)**:
  - 通过滑块或输入框设置任意旋转角度。

### 2.4 配置管理
- **水印模板**:
  - 保存当前所有水印设置为一个模板。
  - 加载、管理和删除已保存的模板。
  - 程序启动时自动加载上次设置或默认模板。

## 3. 技术选型
- **语言**: Python
- **UI 框架**: PyQt6
- **图像处理**: Pillow